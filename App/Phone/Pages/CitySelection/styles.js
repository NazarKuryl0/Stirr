import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  headerBlock: {
    backgroundColor: '#000000',
    height: 70,
    alignItems: 'center',
    paddingTop: 40,
  },
  title: {
    color: '#ffffff',
    fontSize: 14,
  },
  mainBlock: {
    marginLeft: 20,
  },
});

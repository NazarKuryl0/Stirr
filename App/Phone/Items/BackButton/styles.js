import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  backButtonBlock: {
    position: 'absolute',
    top: 20,
    zIndex: 1,
    flexDirection: 'row',
    alignItems: 'center',
  },
  backButton: {
    width: 50,
    height: 55,
    marginRight: -10,
  },
  back: {
    fontSize: 14,
    color: '#ffffff',
  },
});

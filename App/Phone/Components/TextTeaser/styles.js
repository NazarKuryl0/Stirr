import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  subtitleBlock: {
    marginVertical: 33,
    paddingHorizontal: 20,
  },
  subtitleImage: {
    height: 35,
    marginBottom: 22,
  },
  subtitle: {
    color: '#ffffff',
    fontSize: 14,
    textAlign: 'center',
  },
});
